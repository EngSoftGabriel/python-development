# 7. CRUD Completo

Sistema CRUD (Create, Read, Update, Delete) de produtos usando **SQLite**
através do módulo padrão `sqlite3` (sem dependências externas).

- Criação automática da tabela
- Validações básicas
- Banco salvo em `produtos.db`

## Como executar

```bash
python main.py
```
