# 4. API consumindo dados reais

Consulta APIs públicas usando `requests`:

- **ViaCEP** — busca de endereço por CEP
- **AwesomeAPI** — cotação de moedas (USD, EUR, BTC, etc.)

## Como executar

```bash
pip install -r requirements.txt
python main.py
```
